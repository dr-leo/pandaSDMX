1. Check the version number in pandasdmx.__init__.py.


2. Build the source packages. Be sure to first remove any
   development builds:

    $ flit build

3. Upload to PyPI:

    $ twine upload dist/*

4. Wait for the conda bot to open the PR for the conda package, check the skelleton and merge it 
   to build and release the conda package.